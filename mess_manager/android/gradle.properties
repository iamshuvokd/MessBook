org.gradle.jvmargs=-Xmx2G -XX:MaxMetaspaceSize=1G -XX:ReservedCodeCacheSize=256m
android.useAndroidX=true
# Kotlin's incremental compiler crashes computing relative paths when the
# project (D:) and the pub cache (C:) are on different drive letters on Windows.
kotlin.incremental=false
